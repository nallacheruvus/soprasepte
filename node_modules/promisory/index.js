module.exports = {
  resolve(result, callback = null) {
    return callback ? callback(null, result) : Promise.resolve(result);
  },
  
   reject(error, callback = null) {
     return callback ? callback(error) : Promise.reject(error)
  }
}